# @package _global_
defaults:
  - override /algorithm: qvalue_iter
  - override /env: ice_corridor

seed: 42
algorithm:
  gamma: 0.99

eval_env: IceCorridorEval-v0
fsa_name: task1

env:
  add_obj_to_start: True