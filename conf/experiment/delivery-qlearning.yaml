# @package _global_
defaults:
  - override /algorithm: sf
  - override /env: delivery

seed: 42
algorithm:
  gamma: 0.99

eval_env: DeliveryEval-v0
fsa_name: task1

gpi:
  learn:
    avg_td_step: 1e-2
    avg_td_threshold: 1e-3