program: flat_q_learning.py
method: grid
entity: davidguillermo
project: sfcomp
metric:
  goal: maximize
  name: "exp return"
parameters:
  seed:
    values: [25, 142, 745, 4165, 7989, 12324, 45621]
  fsa_name:
    values: ["task1", "task2", "task3", "task4"]

command:
  - ${env}
  - python
  - ${program}
  - +experiment=delivery-flat_q
  - ${args_no_hyphens}

